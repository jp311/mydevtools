#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import com.mysoft.framework.mapper.common.MapperBuilder;
import com.mysoft.framework.mapper.config.MapperProfile;

#parse("File Header.java")
public class ${NAME} extends MapperProfile {
	
	 @Override
    public void registerProfile(MapperBuilder mapperBuilder) {
//		mapperBuilder.createMapper(HtfkApplyForDirect.class, InvoiceLinkBillDTO.class, b -> {
//            b.fieldAToB("BuGuid", "BuGuid");
//         });
	}

}